import { TypeAuditoryPipe } from './type-auditory.pipe';

describe('TypeAuditoryPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeAuditoryPipe();
    expect(pipe).toBeTruthy();
  });
});
